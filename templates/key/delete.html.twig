<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Translator Key</title>
</head>
<body>
<header>
    <h1>Delete This Key</h1>

</header>
<main>

    <div>
        {{ form_errors(formdata) }}
    </div>

    <div>
        <div>
            {{ formdata.text_key.vars.value}}
        </div>

        <div>
            {% for translationmessage in formdata.translationmessages %}

                <p>
                   {{ translationmessage.language.vars.value }}
                    {{ translationmessage.message.vars.value }}</p>


            {% endfor %}

        </div>
        <div>

            {{ form_row(formdata.delete) }}
        </div>
    </div>





    <ul>

        <li><a class="a_link"href="{{path("index_keys")}}">Back</a></li>
    </ul>
</main>
</body>
</html>
